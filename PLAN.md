# ~~Refactor: Shell Command → Full Shell Execution with HITL~~

> **状态：已完成（2026-03-01）**
>
> 此重构已实施。`propose_run_command` 已被 `propose_shell` 替代，`propose_git_write` 已被 `propose_shell` 覆盖。
> 详见 `PROGRESS.md` 中的 Implementation Log 和架构偏移说明。
